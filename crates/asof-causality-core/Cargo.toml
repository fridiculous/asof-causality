[package]
name = "asof-causality-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
name = "asof_causality_core"
path = "src/lib.rs"

[dependencies]
blake3 = { version = "1.8.5", default-features = false }

[dev-dependencies]
proptest = "1.6"
